import {getAssetUrl} from "../../api/client";import type {DirectMessage} from "../../api/messages";
export default function MessageBubble({message,own}:{message:DirectMessage;own:boolean}){return <div className={`flex ${own?'justify-end':'justify-start'}`}><div className={`max-w-[78%] rounded-3xl px-4 py-3 ${own?'bg-brand text-white':'bg-white/80'}`}>{message.status==='deleted'?<span className="text-sm opacity-70">消息已删除</span>:<>{message.image_url&&<img className="mb-2 max-h-80 rounded-2xl" src={getAssetUrl(message.image_url)} alt="私信图片"/>}{message.shared_post&&<a className="mb-2 block rounded-2xl bg-black/10 p-3" href={`/community/post/${message.shared_post.id}`}><strong>{message.shared_post.title}</strong></a>}{message.content&&<p className="whitespace-pre-wrap break-words text-sm leading-6">{message.content}</p>}</>}<p className="mt-1 text-right text-[10px] opacity-60">{new Date(message.created_at).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</p></div></div>}
