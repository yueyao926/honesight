import {Link} from "react-router-dom";import {getAssetUrl} from "../../api/client";import type {UserSearchResult} from "../../api/search";
export default function UserSearchResultCard({user}:{user:UserSearchResult}){return <Link to={`/users/${user.id}`} className="flex items-center gap-4 rounded-2xl bg-white/65 p-4">{user.avatar_url?<img className="h-14 w-14 rounded-full object-cover" src={getAssetUrl(user.avatar_url)} alt=""/>:<span className="flex h-14 w-14 items-center justify-center rounded-full bg-blush">{user.username[0]}</span>}<div className="min-w-0 flex-1"><strong>{user.username}</strong><p className="truncate text-sm text-muted">{user.signature||user.bio||'摄影创作者'}</p><p className="mt-1 text-xs text-muted">{user.follower_count} 粉丝 · {user.work_count} 作品</p></div></Link>}
