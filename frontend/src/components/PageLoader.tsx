export default function PageLoader() {
  return (
    <main className="container-page flex min-h-[50vh] items-center justify-center">
      <p className="text-sm text-muted">页面加载中…</p>
    </main>
  );
}
