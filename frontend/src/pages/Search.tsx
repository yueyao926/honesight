import {useEffect,useState} from "react";import {useSearchParams} from "react-router-dom";import {searchAll,suggestions,type SearchResult} from "../api/search";import PostCard from "../components/community/PostCard";import UserSearchResultCard from "../components/search/UserSearchResultCard";
const TABS=[['all','综合'],['users','用户'],['posts','作品'],['images','图片'],['tags','标签']];
export default function Search(){const [params,setParams]=useSearchParams();const [input,setInput]=useState(params.get('q')||'');const [tab,setTab]=useState(params.get('type')||'all');const [result,setResult]=useState<SearchResult>();const [hints,setHints]=useState<string[]>([]);const [loading,setLoading]=useState(false);const [error,setError]=useState('');const q=params.get('q')||'';useEffect(()=>{if(!input.trim()){setHints([]);return}const t=setTimeout(()=>suggestions(input).then(r=>setHints([...r.history,...r.tags,...r.users,...r.related].filter((x,i,a)=>a.indexOf(x)===i).slice(0,8))).catch(()=>{}),300);return()=>clearTimeout(t)},[input]);useEffect(()=>{if(!q)return;setLoading(true);setError('');searchAll(q).then(setResult).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[q]);function submit(value=input){if(!value.trim())return;setParams({q:value.trim(),type:tab});setHints([])}const posts=tab==='images'?result?.images:result?.posts;return <main className="container-page"><header><p className="section-eyebrow">Discover</p><h1 className="page-title mt-2">搜索摄影灵感</h1><div className="relative mt-6 max-w-3xl"><form className="flex gap-2" onSubmit={e=>{e.preventDefault();submit()}}><input className="input text-base" value={input} maxLength={120} onChange={e=>setInput(e.target.value)} placeholder="例如：雨天夜晚的蓝色街拍"/><button className="btn-primary">搜索</button></form>{hints.length>0&&<div className="absolute z-20 mt-2 w-full rounded-2xl bg-white p-2 shadow-xl">{hints.map(h=><button key={h} className="block w-full rounded-xl px-4 py-2 text-left text-sm hover:bg-blush" onClick={()=>{setInput(h);submit(h)}}>{h}</button>)}</div>}</div></header><nav className="my-7 flex gap-2 overflow-auto border-b border-sand pb-3">{TABS.map(([v,l])=><button key={v} className={tab===v?'rounded-full bg-brand px-5 py-2 text-sm text-white':'btn-ghost'} onClick={()=>{setTab(v);if(q)setParams({q,type:v})}}>{l}</button>)}</nav>{result&&!result.semantic_available&&q&&<p className="mb-5 rounded-2xl bg-sand/50 p-3 text-xs text-muted">当前环境未配置向量模型，结果使用关键词、模糊匹配、标签与摄影语义词扩展。</p>}{loading?<div className="grid gap-4 md:grid-cols-3">{[1,2,3,4,5,6].map(x=><div key={x} className="h-60 animate-pulse rounded-3xl bg-sand"/>)}</div>:error?<div className="card text-ink">{error}</div>:!q?<div className="card text-center text-muted">输入自然语言、用户名、地点或器材开始搜索。</div>:tab==='users'?<div className="grid gap-3 md:grid-cols-2">{result?.users.map(u=><UserSearchResultCard key={u.id} user={u}/>)}</div>:tab==='tags'?<div className="flex flex-wrap gap-3">{result?.tags.map(t=><button key={t.id} className="btn-secondary" onClick={()=>{setInput(t.name);submit(t.name)}}>#{t.name} · {t.usage_count}</button>)}</div>:tab==='all'?<div className="space-y-10"><section><h2 className="font-display text-2xl">摄影用户</h2><div className="mt-4 grid gap-3 md:grid-cols-2">{result?.users.map(u=><UserSearchResultCard key={u.id} user={u}/>)}</div></section><section><h2 className="font-display text-2xl">相关作品</h2><div className="mt-4 columns-2 gap-4 lg:columns-3">{result?.posts.map((p,index)=><PostCard key={p.id} index={index} post={p} onChange={()=>{}}/>)}</div></section></div>:posts?.length?<div className="columns-2 gap-4 lg:columns-3">{posts.map((p,index)=><PostCard key={p.id} index={index} post={p} onChange={()=>{}}/>)}</div>:<div className="card text-center text-muted">暂时没有找到相关内容，可以尝试更换关键词或减少筛选条件。</div>}</main>}
