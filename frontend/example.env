# Copy this file to frontend/.env.

# Dev: use Vite proxy (/api -> http://127.0.0.1:8000)
# Start backend: uvicorn app.main:app --reload --port 8000
VITE_API_BASE_URL=/api
VITE_INSPIRATION_AUTOPLAY_INTERVAL_MS=6000
